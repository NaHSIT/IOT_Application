# IoT backend APP 使用指南

欢迎使用 **IoT backend**。本应用旨在为您提供一个高度自由、无需代码修改即可接入各类物联网设备的移动端监控台。本指南将带您从零开始配置并熟练使用此工具。

---

## 目录
1. [首次登录与基础设置](#1-首次登录与基础设置)
2. [云平台连接配置 (核心)](#2-云平台连接配置-核心)
3. [构建您的专属监控大盘 (核心)](#3-构建您的专属监控大盘-核心)
4. [环境监测与历史数据分析](#4-环境监测与历史数据分析)
5. [警报与异常处理机制](#5-警报与异常处理机制)
6. [常见问题解答 (FAQ)](#6-常见问题解答-faq)

---

## 1. 首次登录与基础设置

APP 安装并打开后，您将看到登录界面。为了保护本地配置数据，本系统设置了基础的进入门槛。

- **默认账号**：`NaHS`
- **默认密码**：`12345678`

登录成功后，您将进入“首页监控大盘”。
*注意：如果您是首次登录，由于系统还没有连接到真实的云平台，首页上方会弹出一个红/黄色的提示横幅，并弹窗要求您去配置 OneNet 参数。*

---

## 2. 云平台连接配置 (核心)

本 APP 默认接入 [中国移动 OneNet 物联网开放平台 (Studio)](https://open.iot.10086.cn/studio)。你需要将您的真实设备与 APP 绑定。

### 步骤 2.1 获取 OneNet 参数
1. 在电脑端登录您的 OneNet Studio 控制台。
2. 在 **“产品管理”** 页面，找到您的产品，复制 **“产品 ID (Product ID)”**。
3. 点击进入产品，在 **“设备列表”** 中找到您想监控的物理设备，复制 **“设备名称 (Device Name)”**。
4. 进入该 **设备详情**，在页面的 **“API调试”** 或 **“鉴权信息”** 区域，生成并复制完整的 **“鉴权 Token (Authorization)”**。
   *(Token 是一长串字符，通常以 `version=...` 开头，请确保复制完整，不要遗漏)*。

### 步骤 2.2 在 APP 中填入参数
1. 在 APP 底部导航栏点击 **“我的”**（设置图标）。
2. 在“系统与云平台”区块下，点击 **“OneNet 平台配置”**。
3. 将上一步获取的 `产品ID`、`设备名称` 和 `Token` 粘贴到对应框内。
4. 点击 **“保存并重启”**。

---

## 3. 构建您的专属监控大盘 (核心)

由于每个人的硬件设备挂载的传感器都不同（有人测温度，有人测甲醛），APP 采用了**“动态数据模型”**设计。你需要告诉 APP：你的设备上报了什么数据？

### 步骤 3.1 添加数据模型
1. 在 **“我的”** 页面，找到 **“数据模型与阈值管理”**。
2. 根据您的需求点击：
   - **“+ 传感器”**：用于添加只读数据（如温度、湿度、电压等）。
   - **“+ 控制器”**：用于添加可操作的下行控制指令（如继电器开关、电机速度设定等）。

### 步骤 3.2 填写关键信息
当弹出“添加设备”面板时，以下几项**必须准确填写**：

*   **内部 ID**：随便起个英文名字（如 `temp1`），用于 APP 内部识别。
*   **设备名称**：显示在卡片上的中文名（如 `机房温度`）。
*   **OneNet 云端标识符 (Cloud Key)**：**极其重要！**这必须与您在 OneNet 平台上定义的**物模型“标识符”完全一字不差**。如果填错，APP 将无法解析数据。
*   **数据类型**：选择设备上报的格式（如 `float` 浮点数、`int32` 整数、`bool` 布尔值）。
*   **图标与颜色**：随您喜好选择，让界面更美观。
*   **报警阈值 (下限/上限)**：*(仅传感器可见)*。例如温度，您可以设定下限 `15`，上限 `30`。当温度超出此范围，手机会立即报警。

保存后，点击底部导航栏的 **“首页”**，您就会看到刚刚配置的传感器变成了一张漂亮的监控卡片！

---

## 4. 环境监测与历史数据分析

### 4.1 实时监测
- **首页概览**：每 30 秒自动同步一次最新数据。点击首页顶部右侧的“刷新”按钮可以立即拉取。
- **详情视图**：点击首页任意一张传感器卡片，进入详情页。详情页会展示该传感器最近几次数据的折线图趋势。

### 4.2 历史数据查询与导出
APP 会在后台静默收集最近 24 小时内的数据。
1. 在首页点击 **“历史数据”** 按钮。
2. 顶部可以左右滑动切换不同的传感器（如温度、湿度）。
3. 页面将绘制出该指标 24 小时内的精密折线图和数据表格。
4. **数据导出**：点击右上角的 **“导出”** 按钮。APP 会生成一份标准的 `.csv` 表格文件。随后手机底部会弹出**原生的分享面板**，您可以直接将其发送至微信、QQ，或保存到手机，甚至直接用 Excel/WPS 打开进行分析。

---

## 5. 警报与异常处理机制

本应用具备边缘计算级别的报警能力，不依赖云端规则，完全由手机本地判断。

### 5.1 触发报警
当传感器数值超过您在“步骤3.2”中设定的阈值时：
- 首页对应卡片会变红，并显示“异常”。
- 首页顶部会弹出红色警报横幅。
- 报警记录会自动记录发生时间、当时数值和超限说明。

### 5.2 抑制虚假报警机制 (防骚扰)
为了避免您的手机被无效报警轰炸，APP 做了以下智能处理：
- **物理设备离线**：当 OneNet 显示物理设备断电离线时，APP 即使拉到了旧的缓存异常数据，也会**自动阻止产生新报警**。
- **持续异常**：如果温度一直保持在 35 度，APP 只会在首次越界时报警一次，直到温度恢复正常后再次越界，才会触发第二次报警。

### 5.3 报警记录管理
点击首页的 **“报警记录”**，可以查看历史发生的所有异常。
- 右上角可“一键已读”或“清空所有记录”。
- 点击单条记录，可直接跳转到发生该报警时的历史数据曲线中分析原因。

---

## 6. 常见问题解答 (FAQ)

**Q: 为什么首页一直显示“正在连接...”，而且卡片数据全是“--”？**
A: 请检查：
1. 您的手机网络是否正常。
2. 去“我的”页面检查 OneNet 平台配置中的 `产品ID`、`设备名` 和 `Token` 是否填错。Token 是有有效期的，过期后需要重新生成。
3. 检查您的物理硬件设备是否真的在线并上报了数据。

**Q: 为什么配置了传感器，但数据还是不显示，或者不更新？**
A: 请去“我的” -> “传感器列表”中点击编辑，仔细检查您填写的 **“OneNet 云端标识符”** 是否与 OneNet 平台后台物模型中的标识符完全一致，区分大小写。

**Q: 为什么我修改了数据模型，但是首页的卡片没有立刻变？**
A: 如果遇到极个别的界面卡顿或混乱，请去“我的”页面滑到最底部，点击红色的 **“恢复默认/清除缓存”** 按钮，APP 将重置状态并自动拉取最新配置。

**Q: 点击导出的 CSV 文件为什么在电脑 Excel 里打开是乱码？**
A: 本 APP 导出的 CSV 文件自带 UTF-8 BOM 签名，现代版本的 Office Excel 或 WPS 打开绝不会乱码。如果您使用极其古老的 Excel 版本，建议先用“记事本”打开文件，另存为 ANSI 编码后再用 Excel 打开。

---
*版本: v1.0.0 | 作者: 硫氢化钠 | 仅供学习参考使用*